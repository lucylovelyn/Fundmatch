{
  "name": "fundmatch",
  "version": "1.0.0",
  "description": "FundMatch - Find your next investor without exposing your startup.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:basesepolia": "hardhat run scripts/deploy.js --network base-sepolia",
    "node": "hardhat node",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "@cofhe/hardhat-plugin": "^0.5.1",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@fhenixprotocol/cofhe-contracts": "^0.1.3",
    "dotenv": "^16.4.5"
  }
}
